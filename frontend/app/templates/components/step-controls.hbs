<div class="back">
  {{#if (and showBack step.hasPrev)}}
    <a {{action "backward"}}>{{backLabel}}</a>
  {{else}}
    &nbsp;
  {{/if}}
</div>
<div class="forward">
  <button class="btn-primary" {{action "forward"}}>{{forwardLabel}}</button>
</div>
