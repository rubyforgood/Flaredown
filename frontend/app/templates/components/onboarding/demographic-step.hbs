{{#form-for for=profile}}

  {{#form-field for="ethnicities"
                label="Ethnic Origin"}}
    {{#power-select-multiple options=ethnicities
                             placeholder="Select all that apply"
                             selected=profile.ethnicities
                             onchange=(action (mut profile.ethnicities))
                             renderInPlace=true
                             searchEnabled=false
                             class="fs-private" as |ethnicity| }}
      {{ethnicity.name}}
    {{/power-select-multiple}}
  {{/form-field}}

  {{#form-field for="dayHabit"
                label="How you spend most of your day?"}}
    {{#power-select options=dayHabits
                    placeholder="Select one option"
                    selected=profile.dayHabit
                    selectedItemComponent="power-select/selected-name"
                    onchange=(action (mut profile.dayHabit))
                    renderInPlace=true
                    searchEnabled=false
                    class="fs-private" as |dayHabit| }}
      {{power-select/dropdown-name-desc option=dayHabit}}
    {{/power-select}}
  {{/form-field}}

  {{#form-field for="educationLevel"
                label="Highest level of education completed"}}
    {{#power-select options=educationLevels
                    placeholder="Select one option"
                    selected=profile.educationLevel
                    onchange=(action (mut profile.educationLevel))
                    renderInPlace=true
                    searchEnabled=false
                    class="fs-private" as |educationLevel| }}
      {{educationLevel.name}}
    {{/power-select}}
  {{/form-field}}

  {{#form-field for="dayWalkingHours"
                label="How many hours a day do you spend walking or exercising, on average?"}}
    {{input type="number"   id="input-num" keyUp=(action "validateNumber") value=profile.dayWalkingHours  maxlength=2 class="fs-private" min=0 max=24 pattern="[0-9]"}}
  {{/form-field}}

{{/form-for}}

{{step-controls step=step
                onBackward=(action "goBack")
                onForward=(action "completeStep") }}
