<h3 class="centered page-title">{{t "history.step.initial.title"}}</h3>

{{#if createPatternStep}}
  {{pattern/create-step}}
{{else}}

 <div class="flaredown-transparent-box">
    <p class="centered">{{t "history.step.initial.text"}}</p>

    {{pattern/svg-initial}}
    <div class="history-container">
      <button class="history-btn btn" {{action "toggleCreatePatternStep"}}>
        {{t "history.step.initial.buttonText"}}
      </button>
    </div>
  </div>
{{/if}}


{{!-- <div class="btn-group vert-offset-top-1 vert-offset-bottom-1">
  <button {{action "showChart"}} class={{if journalIsVisible "" "active"}}>Charts</button>
  <button {{action "showJournal"}} class={{if journalIsVisible "active" ""}}>Journal</button>
</div>

{{#if journalIsVisible}}
  {{journal-list}}
{{else}}
  {{health-chart onDateClicked=(action "routeToCheckin")}}
{{/if}} --}}
