{{#form-for for=model onSubmit="saveProfile"}}

  {{#unless forOnboarding}}
    {{#form-field for="screenName" label="Screen Name"}}
      {{input value=model.screenName placeholder="This is how other users would see you"}}
    {{/form-field}}
  {{/unless}}

  {{#form-field for="country" label="Country"}}
    {{select-field resource="country" async=false
                   selection=model.country
                   placeholder="Select a country"}}
  {{/form-field}}

  {{#form-field for="birthDate" label="Birth Date"}}
    {{input type="date" value=model.birthDate}}
  {{/form-field}}

  {{#form-field for="sex" label="Sex"}}
    {{#each sexes as |sex|}}
      {{#radio-button name="sex" value=sex.id groupValue=selectedSexId changed="sexChanged"}}
        {{sex.name}}
      {{/radio-button}}
    {{/each}}
  {{/form-field}}

  {{#unless forOnboarding}}
    <br>
    {{input type="submit" value="Save"}}
  {{/unless}}

{{/form-for}}
