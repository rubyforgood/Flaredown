{{#form-for for=profile}}
 {{#radio-button
      value=false
      groupValue=profile.checkinReminder
      changed=( action (mut profile.checkinReminder))}}
      {{reminderOff}}
  {{/radio-button}}

  {{#radio-button
      value=true
      groupValue=profile.checkinReminder
      changed=( action (mut profile.checkinReminder))}}
      {{reminderOn}}
  {{/radio-button}}
{{/form-for}}
