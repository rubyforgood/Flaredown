{{checkin-trackeds checkin=model.checkin trackableType="condition"}}
{{step-controls step=model.currentStep
                onBackward=(action "goBack")
                onForward=(action "completeStep") }}
