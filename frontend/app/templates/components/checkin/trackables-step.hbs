{{checkin-trackeds trackeds=trackeds
                   trackableType=trackableType
                   onTrackedRemoved=(action "rememberRemovedTracked")
                   onTrackedAdded=(action "rememberAddedTracked")}}

{{step-controls step=model.currentStep
                onBackward=(action "goBack")
                onForward=(action "completeStep") }}
