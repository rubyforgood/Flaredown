<h4 {{action "titleClicked"}}>
  {{label}}
  {{#unless disabled}}
    <span class="x-to-remove" {{action "xClick"}}>×</span>
  {{/unless}}
</h4>
<div>
  <ul class="pips">
    {{#each pips as |pip|}}
      {{pip-tag value=pip.value selected=selectedValue current=currentValue modelBgClass=model.bgClass
                  onClick=(action "setSelectedValue")
                  onMouseEnter=(action "setCurrentValue")
                  onMouseLeave=(action "unsetCurrentValue")}}
    {{/each}}
  </ul>
</div>
