<header class="centered navigation" role="banner">
  {{#if session.isAuthenticated}}
    {{navigation-bar}}
  {{else}}
    <div>
      <img src="{{rootURL}}assets/logo.svg" class="img-logo">
      <h4>{{capitalize currentPath}}</h4>
    </div>
  {{/if}}
</header>
